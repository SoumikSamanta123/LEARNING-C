//You are given two variables height1 and height2 - output 0 if they are equal else 1.
#include <stdio.h>

int main() {
  int height1 = 15;
  int height2 = 13;
  // Update the '_' below to solve the problem
  printf("%d", height1 != height2); // returns 1 (true) because 15 is not equal to 13
  return 0;
}